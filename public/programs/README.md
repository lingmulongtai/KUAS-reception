# プログラム画像

プログラムカードのサムネイルに使う画像をここに置く。

- 管理画面 → プログラム編集 → 「画像」欄に `public/programs/ファイル名.jpg` と入力する
- 空欄のあいだは番号入りのプレースホルダが表示される
- 推奨サイズ: 横 800px 以上、16:9。カード側で切り抜くので、被写体は中央寄りに
- 形式: jpg / png / webp

画像そのものは localStorage には入らない（パス文字列だけを保存する）ため、
容量を気にせず置ける。ただしファイルを消すとサムネイルは壊れる。
